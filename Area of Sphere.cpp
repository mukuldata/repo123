#include<stdio.h>
#include<conio.h>
int main()
{
	float area,radius;
	float pi=3.142;
    printf("\n enter the value of radius");
    scanf("%d%d",&radius,&pi);
    area=4*pi*radius*radius;
    printf("\the res is=%f",area);
    getch();
}
